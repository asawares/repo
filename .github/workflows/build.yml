name: Build ASI Plugin

on: [push, pull_request]

jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup build tools
      uses: microsoft/setup-msbuild@v1

    - name: Build Detours
      run: |
        cd Detours-main
        nmake

    - name: Verify Detours
      run: |
        ls Detours-main/src/detours.h
        ls Detours-main/lib.X64/detours.lib

    - name: Configure and build
      run: |
        mkdir build
        cd build
        cmake -G "Visual Studio 17 2022" -A x64 -DDETOURS_INCLUDE_DIR=../Detours-main/src -DDETOURS_LIBRARY=../Detours-main/lib.X64/detours.lib ..
        cmake --build . --config Release --target SAMPInjection -- /m

    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: SAMPInjection.asi
        path: build/Release/SAMPInjection.asi
